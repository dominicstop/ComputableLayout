import XCTest
@testable import ComputableLayout

final class ComputableLayoutTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ComputableLayout().text, "Hello, World!")
    }
}
